#include <iostream>
#include<vector>
#include <bits/stdc++.h>
using namespace std;
int main() {
     int n ;
     cin>>n;
     int dec = 0, i = 0, rem;

  while (n!=0) {
    rem = n % 10;
    n /= 10;
    dec += rem * pow(2, i);
    ++i;
  }

  cout<<dec;
     
    return 0;
}
