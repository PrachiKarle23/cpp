#include <iostream>
#include<cmath>

using namespace std;
int main() {
     int no ;
     cin>>no;
int ans=0,i=0;
     while (no != 0) {
        int rem = no % 2;
        ans=ans+ rem * pow(10,i);
        no = no / 2;
        i++;
    }
    
    cout<<ans;
    return 0;
}
