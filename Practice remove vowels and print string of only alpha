// Online C++ compiler to run C++ program online
#include <iostream>
#include <set>
using namespace std;
int main() {
    
    string str = "Gee$ksfor$geeks";
    // int j=0;
    // multiset<int>a={'a','e','i','o','u','A','E','I','O','U'};
    // for(int i=0;i<str.length();i++)
    // {
    //     if(a.find(str[i]) == a.end()){
    //         str[j++]=str[i];
    //     }
    // }
    // while(j<str.length())
    // {
    //     str[j++]=' ';
    // }
    // cout<<str<<endl;
    string result="";
    for(int i=0;i<str.length();i++)
    {
        if(isalpha(str[i])){
            result+=str[i];
        }
    }
    cout<<result<<endl;
    return 0;
}
